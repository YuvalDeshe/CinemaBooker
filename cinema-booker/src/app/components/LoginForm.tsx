'use client';
import User from "@/models/userSchema"

type LoginFormProps = {
    handleLogin: (user: typeof User) => void
}

export default function LoginForm() {
    /* insert JSX for the login form. */
}